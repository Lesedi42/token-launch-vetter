services:
  - type: web
    name: token-launch-vetter
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 3000
      - key: WALLET_ADDRESS
        sync: false
      - key: NODE_ENV
        value: production
